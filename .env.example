# Minimal env file for the Mission Control dashboard
# Only the variables below are required for the MVP.
# Future variables are listed with a comment – you can uncomment and fill them later.

TASK_USER=          # Basic‑Auth username
TASK_PASS=          # Basic‑Auth password

# The following variables are declared for future use; you can add them later:
# RAILWAY_TOKEN=     # Railway API token (automation / git push)
# TG_BOT_TOKEN=      # Telegram bot token (alerts)
# ALERT_WEBHOOK=     # Webhook URL for extra alert channels